# legal_counter_argumentation_miniproject
Mini-project that acts as an advisor for preparing the defense of a given case. Based on a provided document, it uses a reasoning model to generate a set of arguments the opposing side might use, along with counter-arguments the user can use in their own defense.
